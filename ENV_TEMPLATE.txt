# Copy this content to .env file
# OpenAI Configuration (Recommended for better query parsing and JSON responses)

# Core Service Config
ENVIRONMENT=prod
API_PREFIX=/api/v1
VECTOR_TOP_K=20

# Qdrant Configuration
QDRANT_HOST=qdrant
QDRANT_PORT=6333

# Embedding Configuration - Using Ollama (Free, Local)
# Alternative: Use OpenAI embeddings by setting EMBEDDING_PROVIDER=openai
EMBEDDING_PROVIDER=ollama
EMBEDDING_DIM=768
OLLAMA_BASE_URL=http://ollama:11434
OLLAMA_EMBED_MODEL=all-minilm:16-v2

# Agent Configuration - Using OpenAI for LLM (Recommended for better results)
AGENT_ENABLED=true
LLM_PROVIDER=openai
LLM_MODEL=gpt-3.5-turbo
OPENAI_API_KEY=sk-your-openai-api-key-here
LLM_TEMPERATURE=0.3
LLM_MAX_TOKENS=500

# Alternative: If you want to use Ollama for LLM instead
# LLM_PROVIDER=ollama
# OLLAMA_LLM_MODEL=llama2

# UI Configuration
UPLOAD=local

